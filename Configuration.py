config = {
    "Model": {
        "MED_GEMMA_PATH": "medgemma-1.5-4b-it",  # Path to the MedGemma model
        "MAX_TOKEN_LENGTH": 500,  # Maximum generation length for the model
        "ACCELERATOR": "cpu",  # Device to run the model on (e.g., "cpu", "cuda", "mps")
    }
}
